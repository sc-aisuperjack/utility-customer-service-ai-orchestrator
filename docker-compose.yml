services:
  app:
    build: .
    container_name: responsible-contact-centre-ai-lab
    ports:
      - "8001:8001"
    environment:
      MODEL_PROVIDER: local
      PROMPT_VERSION: utility_assistant_v1
