# Voice and IVR Design

Voice is not chat with a speaker attached. Voice needs short turns, ASR error handling, barge-in, DTMF fallback, explicit confirmation and low latency.

Confirm meter readings, dates, payment amounts, account references and appointment slots.
